server.port: 5601
server.host: "{{ip or localhost or 0.0.0.0}}"
server.maxPayloadBytes: 1048576
server.name: "{{HOSTNAME}}"
server.defaultRoute: "/app/kibana"
elasticsearch.url: "http://localhost:9200"
kibana.index: ".kibana"
